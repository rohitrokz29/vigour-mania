import React from 'react'

export const Spots = ({title,description}) => {
  return (<>
    <div>{title}</div>
    <div>{description}</div>
    </>
  )
}

export const Evolve = ({title,description}) => {
  return (
<>
    <div>{title}</div>
    <div>{description}</div>
</>
  )
}
